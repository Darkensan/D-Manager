
Example of an IPv6 network configuration:

Use the belove scheme to populate interfaces.conf
	
How to edit .cfg file:
- Some vps build has the file registered under a different name and folder.
- Default: /etc/network/interfaces.cfg
- VPS Build: /etc/network/interfaces.d/*.cfg

use this command:

nano /etc/network/interfaces.d/50-cloud-init.cfg


Once done editing, run this command to reset the network interfaces once done editing .cfg:

systemctl restart networking


-----------------------------------------------------------------------------------
# This file is generated from information provided by
# the datasource.  Changes to it will not persist across an instance.
# To disable cloud-init's network configuration capabilities, write a file
# /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:
# network: {config: disabled}
auto lo
iface lo inet loopback

auto ens3
iface ens3 inet dhcp
iface ens3 inet6 static
                        address xxxx:xxxx:xxx:xxxx:0:0:0:xxxx
                        netmask 64
                        up /sbin/ip -6 addr add dev ens3 xxx:xxx:xxx:xxxx::yyy*
                        up /sbin/ip -6 addr add dev ens3 xxxx:xxxx:xxx:xxxx::yyy*
                        up /sbin/ip -6 addr add dev ens3 xxxx:xxxx:xxx:xxxx::yyy*
						***add as many line as needed ***

						
*yyy = any combination of numbers some letters ( a, b, c, d, - only worked for me)

i.e: ::d01 - ::ad1 - ::3bd - ::02d  and so on

QT Wallet: set the IPv6 of the node as follow: xxxx:xxxx:xxx:xxxx::xxxx:d01
(replacing all the 0 with :: before last number then add the 3-4 extra code of choice)


-------------------------------------------------------------------------------
						
